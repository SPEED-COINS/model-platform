specVersion: "v1beta"
serviceId: "model-platform"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "model-platform-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
